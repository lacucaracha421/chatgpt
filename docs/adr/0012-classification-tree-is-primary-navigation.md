# 분류 트리를 주 탐색 수단으로 사용한다

데스크톱 앱의 왼쪽 사이드바에 계층형 분류 트리를 두고 이를 자산과 작품을 찾는 기본 경로로 사용한다. 상위 분류를 선택하면 기본적으로 모든 하위 분류의 자산을 포함하되, `하위 분류의 자산 포함` 토글을 끄면 직접 연결된 자산만 표시한다. 검색은 항목 위치를 모를 때 사용하는 단순한 보조 기능으로 제한하고, 첫 버전에는 AI 이미지 검색을 포함하지 않는다. 실제 사용에서 검색어 입력보다 익숙한 분류 위치를 따라가는 탐색이 더 빠르다는 사용자 경험을 반영한 결정이다.
