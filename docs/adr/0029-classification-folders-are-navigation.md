# 분류 항목을 폴더처럼 조작하되 저장 위치로 만들지 않는다

사용자에게는 계층형 분류 항목을 익숙한 폴더 트리로 표현하지만, 분류 폴더는 실제 파일 저장 위치나 단일 소속을 의미하지 않는다. 자산은 여러 분류 폴더에 연결될 수 있고 자산을 폴더로 드래그하면 기존 연결을 유지한 채 새 연결을 추가한다. 하위 폴더가 없는 분류 폴더를 삭제할 때는 직접 연결된 자산을 상위 폴더로 이전한다. 최상위 폴더는 상위 폴더가 없으므로 해당 연결만 제거하고 자산은 보존한다. 하위 폴더가 있으면 자동 승격이나 병합 대신 삭제를 거부한다.
