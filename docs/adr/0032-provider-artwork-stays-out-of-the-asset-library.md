# Provider artwork stays outside the Asset Library

Artwork downloaded to present a Work is stored as WorkArtwork with its own local lifecycle instead of being ingested automatically as an Asset. This keeps provider covers available offline without polluting the user's collected-media library, while preserving a future explicit action that can promote selected artwork through normal Asset ingestion.
